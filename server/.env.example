# Application
PORT=3000
# comma-separated list of origins allowed for CORS. Leave empty for "*"
CORS_ORIGINS=http://localhost:5173

# Database configuration
DB_HOST=localhost
DB_PORT=5432
DB_NAME=chatgptcarbon
DB_USER=postgres
DB_PASSWORD=postgres
# Enable SQL logging (true/false)
DB_LOGGING=false
